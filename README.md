> [!CAUTION]
> This package is no longer needed as you can now use [`axios.isAxiosError`](https://github.com/axios/axios#typescript).

# is-axios-error

A type guard to check if an error object in an Axios error.
